# MLOps with Agentic AI - Session 8: Complete CI/CD Pipeline
# Author: Amey Talkatkar
# Repository: https://github.com/ameytrainer/ml-forecast-system

# DagsHub MLflow Configuration
# Copy this file to .env and fill in your credentials

# DagsHub credentials (get from: https://dagshub.com/user/settings/tokens)
DAGSHUB_USERNAME=your-dagshub-username
DAGSHUB_TOKEN=your-dagshub-token
DAGSHUB_REPO=ml-forecast-system

# MLflow Tracking (auto-generated from DagsHub)
MLFLOW_TRACKING_URI=https://dagshub.com/${DAGSHUB_USERNAME}/${DAGSHUB_REPO}.mlflow
MLFLOW_TRACKING_USERNAME=${DAGSHUB_USERNAME}
MLFLOW_TRACKING_PASSWORD=${DAGSHUB_TOKEN}

# DVC Remote (auto-generated from DagsHub)
DVC_REMOTE_URL=https://dagshub.com/${DAGSHUB_USERNAME}/${DAGSHUB_REPO}.dvc

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Replace 'your-dagshub-username' with your actual username
# 3. Replace 'your-dagshub-token' with token from DagsHub settings
# 4. Keep .env file private (already in .gitignore)